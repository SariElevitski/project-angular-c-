export class Category {

   
  id: number;         // מזהה הקטגוריה
  name: string;       // שם הקטגוריה

  constructor(
    id: number,
    name: string
  ) {
    this.id = id;
    this.name = name;
  }
}

